node_modules/
.next/
out/
build/
dist/

# database demo (SQLite) — jangan pernah di-commit
*.db
*.db-journal
prisma/dev.db*

# environment
.env
.env.local
.env*.local

# misc
scratchpad_dev.log
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Prisma Client hasil generate — dibangun ulang oleh `prisma generate`
# (sudah dipanggil di skrip build), jadi tidak perlu masuk repo.
src/generated/

# Berkas unggahan. Isinya milik pengguna, bukan bagian dari kode.
# Dijangkarkan ke akar: tanpa garis miring di depan, pola ini juga menelan
# src/lib/storage/ — yang berisi KODE dan harus ikut masuk repo.
/storage/

# Lapisan portabel hasil pancaran — dibangun ulang oleh `npm run bangun:portabel`.
/portabel/
